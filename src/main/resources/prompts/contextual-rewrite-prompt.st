请将用户查询重写为更清晰明确的意图表达，便于知识库检索。如果原查询已经很明确，则无需重写。

对话历史：
{history}

当前查询：
{query}

重写规则：
1. 简短的金额数字 → 补全为借款意图（如"500" → "借款500元"）
2. 模糊抱怨 → 明确具体问题（如"借不了" → "借款失败原因"）
3. 口语化表达 → 标准化术语（如"提额度" → "提升额度"）
4. 利用历史上下文补全省略信息
5. 已有借款方案时的金额修改 → 明确为方案调整（如"我想借3000元" → "修改借款金额为3000元"）
6. 确认/应答类短语（如"好的"、"好"、"嗯"、"行"、"可以"）保持不变，不要推断或改变意图

中文语法约束：
- 重写后的查询必须符合标准中文语法结构
- 使用完整的主谓宾结构，避免语法残缺
- 动词和宾语搭配要准确（如"查询额度"而非"查询的额度"）
- 数量词后必须加合适的量词（如"500元"而非"500块"）
- 避免口语化的语气词和助词（如"呢"、"吧"、"啊"等）
- 使用规范的金融术语（如"借款"而非"借钱"，"额度"而非"限额"）
- 保持语义完整性，避免歧义表达

语言规范要求：
- 动作类查询使用动宾结构：[动词]+[宾语]（如"查询额度"、"申请借款"）
- 状态类查询使用主谓结构：[主语]+[状态]（如"借款失败"、"额度不足"）
- 条件类查询使用条件句式：[条件]+[查询内容]（如"月收入5000元的借款额度"）
- 时间相关查询明确时间范围：[时间]+[动作/状态]（如"当前借款状态"、"历史还款记录"）

只输出重写后的查询，不要解释。

示例：
基础重写：
- "借不了" → "借款失败原因"
- "500" → "借款500元"
- "额度" → "查询额度"
- "提前还" → "提前还款"
- "借200" → "申请借款200元"
- "为什么失败" → "借款失败原因"
- "提额" → "提升额度"

语法优化示例：
- "我的额度是多少啊" → "查询当前额度"
- "能借多少钱呢" → "查询可借款额度"
- "利息怎么算的" → "查询利息计算方式"
- "还款日期咋改" → "修改还款日期"
- "月收入8000能借多少" → "月收入8000元的借款额度"
- "上个月的账单" → "查询上月还款账单"

保持不变的情况：
- "利息太高了" → "利息太高了"（已经规范）
- "怎么提额度" → "怎么提额度"（已经规范）
- "好的" → "好的"（确认类短语）
- "查询额度" → "查询额度"（已经标准化）

